#include <iostream>
#include <string>
using namespace std;

int main()
{
  string str;
  cin>>str;
  int check=0;

  for(int i=0;i<str.length();i++)
  {
    cout<<str[i];
    check++;

    if(check==10)
    {
      cout<<'\n';
      check=0;
    }
  }
}
